<h1>check out what other users have posted..</h1>

<!-- <%# @posts.each do |post| %>
  <%# next if post.user.id == @current_user.id %>
  <%=# link_to post.image, post_path(post.id) %><br>
  <%=# user.profile %>
<%# end %> -->


<% @posts.each do |post| %>
  <% next if post.user.id == @current_user.id %>

      <%= link_to (image_tag post.photograph, posts_path(@post.id) %>
        <%= @post.title %>
        <%= @post.caption %>

      <% if post.comments %>
        <% post.comments.each do |comment| %>

              <%= comment.user.user_name %>

              <%= comment.content %>

            <% if comment.user == current_user %>
              <%= link_to post_comment_path(post, comment), method: :delete, data: { confirm: "Are you sure?" } %>
            <% end %>

        <% end %>
      <% end %>

        <%= form_for [post, post.comments.new] do |f| %>
          <%= f.text_field :content, placeholder: 'Add a comment...' %>
        <% end %>

<% end %>
